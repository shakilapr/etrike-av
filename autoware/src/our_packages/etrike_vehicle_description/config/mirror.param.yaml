/**:
  ros__parameters:
    # Mirror dimensions have not been measured on the physical E-Trike.
    # Zero offsets disable mirror-specific point-cloud cropping until calibrated.
    min_longitudinal_offset: 0.0
    max_longitudinal_offset: 0.0
    min_lateral_offset: 0.0
    max_lateral_offset: 0.0
    min_height_offset: 0.0
    max_height_offset: 0.0
